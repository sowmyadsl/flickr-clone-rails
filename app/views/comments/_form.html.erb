<%= render "layouts/errors", :object => @comment %>

<%= form_for @comment, url: image_comments_path(image) do |f| %>
  <%= f.label :body %>
  <%= f.text_area :body, placeholder: "Add a comment" %>
  <br>
  <%= f.submit class: 'btn btn-info' %>
<% end %>
